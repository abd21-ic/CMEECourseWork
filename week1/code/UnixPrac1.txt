#1 - Count how many lines there are in each file - wc ../data/fasta/407228326.fasta && wc ../data/fasta/407228412.fasta && wc ../data/fasta/E.coli.fasta
#2 - Print everything starting from the second line for the E. coli genome - tail -n +2 ../data/fasta/E.coli.fasta
#3 - Count the sequence length of this genome - grep -v '^>' ../data/fasta/E.coli.fasta | tr -d '\n' | wc -c
#4 - Count the matches of a particular sequence, “ATGC” in the genome of E. coli (hint: Start by removing the first line and removing newline characters) - grep -v '^>' ../data/fasta/E.coli.fasta | tr -d '\n'| grep -o "ATGC" ../data/fasta/E.coli.fasta "ATGC" | wc -l
#5 - Compute the AT/GC ratio. That is, the (A+T)/(G+C) ratio (as a single number). This is a summary measure of base composition of double-stranded DNA. DNA from different organisms and lineages has different ratios of the A-to-T and G-to-C base pairs (google “Chargaff’s rule”). For example, DNA from organisms that live in hot springs have a higher GC content, the GC base pair is more thermally stable. - seq=$(grep -v '^>' ../data/fasta/E.coli.fasta | tr -d '\n')
A=$(echo "$seq" | grep -o 'A' | wc -l)
T=$(echo "$seq" | grep -o 'T' | wc -l)
G=$(echo "$seq" | grep -o 'G' | wc -l)
C=$(echo "$seq" | grep -o 'C' | wc -l)
Y=$(echo "$seq" | grep -o 'Y' | wc -l)
R=$(echo "$seq" | grep -o 'R' | wc -l)
ATGC_ratio=$(echo "scale=3; ($A + $T + $Y + $R) / ($G + $C + $Y + $R)" | bc)
echo $ATGC_ratio